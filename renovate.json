{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    "helpers:pinGitHubActionDigests"
  ],
  "addLabels": ["dependencies"],
  "automergeType": "branch",
  "platformAutomerge": true,
  "prConcurrentLimit": 5,
  "minimumReleaseAge": "3 days",
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"],
    "automerge": false
  },
  "osvVulnerabilityAlerts": true,
  "packageRules": [
    {
      "description": "Hands-off: branch-automerge every non-major update (all managers, incl. GitHub Actions) once CI passes and the release-age soak elapses",
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "automerge": true
    },
    {
      "description": "Major updates are the only ones that open a PR for manual review",
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "labels": ["dependencies", "major-update"]
    },
    {
      "description": "The wrapped Provenant image tag is pinned per action release, not by Renovate; bump it deliberately when cutting a new action version so consumers get a known image.",
      "matchManagers": ["dockerfile"],
      "matchPackageNames": ["ghcr.io/getprovenant/provenant"],
      "enabled": false
    }
  ]
}
